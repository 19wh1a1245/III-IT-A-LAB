bool isPalindrome(int x){
    long long int rem,reverse=0,temp;
    temp=x;
    while(x>0)
    {
        rem = x%10;
        reverse = reverse*10 + rem;
        x = x/10;
    }
    if(reverse==temp)
        return true;
      
      return false;
    
}
